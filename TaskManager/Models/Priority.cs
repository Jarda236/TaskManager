namespace TaskManager.Models
{
    /// <summary>
    /// Prirority of the task, needs to be from highest to lowest, because of calculations
    /// </summary>
    public enum Priority
    {
        High,
        Medium,
        Low
    }
}
